#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("copycat.in", "r", stdin);
    freopen("copycat.out", "w", stdout);
    int t;
    string s;
    cin >> t;
    while (t--) {
        cin >> s;
        cout << s << endl;
    }
    return 0;
}