#include <bits/stdc++.h>

using namespace std;

int n;

int main() {
    while (cin >> n, n) {
        for (int i = 1; i <= n; i++) {
            cout << i << ' ';
        }
        cout << endl;
    }
    return 0;
}
