// R39590203

#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b, c, d, f, e, g, h, i;
    cin >> a >> b >> c >> d >> e >> f >> g >> h >> i;
    if ((h && 50 + a + b + c + d + e + f + g + 5 >= i) || (50 + a + b + c + d + e + f + g >= i)) {
        cout << "AKIOI" << endl;
    }
    else {
        cout << "AFO" << endl;
    }
    return 0;
}
